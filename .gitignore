# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv
venv/
env/

# Model checkpoints — large binary files, regenerate with train_bc.py / train_bc_gru.py
models/*.pt
models/*.pth
models/*.ckpt

# Dataset files — large and regenerable, see dataset/README.md
dataset/*.parquet
dataset/*.npz
dataset/*.csv
dataset/*.h5
data/*.parquet
data/*.npz

# Training artefacts — regenerated by train_bc.py / train_bc_gru.py / compare_models.py
# GIFs kept intentionally: models/pick_place_rollout_*.gif
models/training_curves*.png
models/pick_place_analysis.png
models/training_metrics*.json
models/comparison_results.json

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Experiment tracking (no keys should exist here, but exclude any local configs)
wandb/
.wandb/
mlruns/
runs/
